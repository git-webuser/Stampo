// SettingsView.swift is intentionally empty.
// The settings window is managed by SettingsWindowController,
// which uses NSTabViewController with toolbar style to host
// each settings section (GeneralSettingsView, CaptureSettingsView, etc.)
// directly as NSHostingController tabs — giving proper macOS icons in the toolbar.
